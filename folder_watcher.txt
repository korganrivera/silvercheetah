
list of files that existed last time I checked.
    files_old.txt

list of files that exist now.
    find ~/Dropbox/cycling\ files/csvFiles -type f -maxdepth 1 > files_now.txt

difference file.
    sort files_now.txt files_old.txt | uniq -u > files_diff.txt

if (wc -l < files_diff.txt) > 0
    push filenames from files_diff.txt to silvercheetah
    notify-send "silvercheetah updated!"
cp files_now.txt files_old.txt

cron the above or inotifywait for file changes.





